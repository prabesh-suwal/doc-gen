# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=docgen
DB_USER=prabesh
DB_PASSWORD=Bhaktapur@123
DB_POOL_SIZE=20

# DocGen Environment Configuration
# Copy this file to .env and update with your values

# Server Configuration
PORT=3000
HOST=0.0.0.0
BASE_URL=http://13.127.210.14:3000

# JWT Secrets (CHANGE THESE IN PRODUCTION!)
JWT_ACCESS_SECRET=your-super-secret-access-token-key-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-token-key-change-in-production

# OnlyOffice Configuration
ONLYOFFICE_ENABLED=true
ONLYOFFICE_URL=http://13.127.210.14:8080
ONLYOFFICE_CALLBACK_URL=http://13.127.210.14:3000/api/editor/callback
ONLYOFFICE_JWT_ENABLED=false
ONLYOFFICE_JWT_SECRET=

# Storage Paths (optional - defaults to project directories)
# STORAGE_PATH=/path/to/templates
# OUTPUT_PATH=/path/to/output
# TEMP_PATH=/path/to/temp
# EDITING_PATH=/path/to/editing

# LibreOffice Path (optional - defaults to 'soffice')
# LIBREOFFICE_PATH=/usr/bin/soffice

# Normalization (optional - defaults to true)
# ENABLE_NORMALIZATION=true

# Logging (optional - defaults to 'info')
# LOG_LEVEL=debug
